#!/usr/bin/env node

// Verify that you got required number of arguments
if(process.argv.length < 3) {
	console.log('Usage: git branch-serve branch-name');
	process.exit();
}

require('../lib/branch-serve');
